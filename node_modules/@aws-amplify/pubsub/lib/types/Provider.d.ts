import * as Observable from '@aws-amplify/pubsub/lib/types/node_modules/zen-observable';
import { ProvidertOptions } from '@aws-amplify/pubsub/lib/types/PubSub';
export interface PubSubProvider {
    configure(config: object): object;
    getCategory(): string;
    getProviderName(): string;
    publish(topics: string[] | string, msg: any, options?: ProvidertOptions): void;
    subscribe(topics: string[] | string, options?: ProvidertOptions): Observable<any>;
}
